# These mirror the dependencies in ../pyproject.toml. main.py/mlb_api.py/
# generic_api.py are NOT installed as a package here - they're imported
# directly from the repo root (see server.py), since this wrapper is
# deployed as part of the same repo it wraps rather than as a separate
# pip dependency.
fastmcp>=2.10.6
fastapi>=0.115.12
python-mlb-statsapi>=0.5.26
uvicorn[standard]>=0.24.0
pybaseball>=2.2.7
starlette~=1.3.1
PyJWT[crypto]~=2.13.0
